import { member } from './member'
import { board } from './board'
import { forms } from './forms'

const rootState = {
  selectedLang: 'fr',
  member,
  board,
  forms,
}

export default rootState
