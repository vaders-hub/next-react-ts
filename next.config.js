/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: {
  //   removeConsole: {
  //     exclude: ["error"],
  //   },
  // },
};

module.exports = nextConfig;
